name: SAST (Static Analysis)

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  semgrep:
    name: Semgrep Scan
    runs-on: ubuntu-latest
    container:
      image: semgrep/semgrep
    steps:
      - name: Checkout code
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Run Semgrep
        run: |
          set -o pipefail
          semgrep scan --config auto --error 2>&1 | tee semgrep-results.txt

      - name: Upload Semgrep Report
        uses: actions/upload-artifact@0b2256b8c012f0874dc3919b9ab1d47786821213 # v4.3.0
        if: always()
        with:
          name: semgrep-results
          path: semgrep-results.txt
          retention-days: 7
